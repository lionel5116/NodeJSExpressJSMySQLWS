const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "root",
      password: "Mag17615@",
      database: "hisdnutritionalservices",
    },
    listPerPage: 1000,
  };
  module.exports = config;